#include <stdio.h>
void main()
{
    int a,b;
    printf("type 2 grades of yours: ");
    scanf("%d%d",&a,&b);
    if (((a+b)/2)>90)
    {
        printf("good student");
    }
    else
    {
        printf("avg student");
    }
}
