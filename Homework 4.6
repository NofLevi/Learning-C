#include <stdio.h>
int main()
{
	int x,y,z,temp;
	printf("type 3 numbers: ");
	scanf("%d%d%d",&x,&y,&z);
	if ((x>y)&&(x>z))
	{
	temp=x;
	x=z;
	z=temp;
	printf("the biggest number is: %d",z);
}
else if ((y>x)&&(y>z))
{
	temp=y;
	y=z;
	z=temp;
	printf("the biggest number is: %d",z);
}
else 
{
	printf("the biggest number is: %d",z);
}
}
