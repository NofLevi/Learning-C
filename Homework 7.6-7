#include <stdio.h>
void main()
{
    int a,temp;
    printf("Enter 1 positive number: ");
    scanf("%d",&a);
    temp = a;
    a = a - 1;
    do
    {
        printf("why did u put negative number ?\nEnter 1 positive number:");
        scanf("%d",a);
    }
    while(a<0);
        do 
    {
         temp = temp * a;
        a--;
    }
    while(a>0);
    printf("The answer is:%d",temp);
    }
