#include <stdio.h>
int func(char st[],char tav)
{	
	static int count = 0;
	if (*st)
	{
		if (*st== tav)
		count++;
		//printf("%c",*st);
		st++;
		func(st,tav);
	}
	return count;
}
int main()
{
	char st[10]= "helloll";
	char tav ='l';
	//func(st,tav);
	printf("%d ",func(st,tav)); 
} 
