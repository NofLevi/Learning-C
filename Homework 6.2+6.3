#include <stdio.h>
void main()
{
char ch;
printf("Enter 1 small english letter: ");
ch = getchar();
if ((ch>=97)&&(ch<=122))
{
    ch = (ch - 32);
    putchar(ch);
}
else
{
    printf("u wrote a wrong letter im bringing you back to the beggining remember to put small english letter only");
}
}
