#include <stdio.h>
void
main ()
{
  float x, y, sum;
  char op;
  int mod,xint,yint;
  printf ("Enter Number mathematical operator then another number: ");
  scanf ("%f%c%f", &x, &op, &y);
  switch (op)
    {
    case '+':
      printf ("%f", x + y);
      break;
    case '-':
      printf ("%f", x - y);
      break;
    case '*':
      printf ("%f", x * y);
      break;
    case '/':
      printf ("%f", x / y);
      break;
    case '%':
    xint=x;
    yint=y;
      printf("%d",xint % yint);
      break;
    }
}
