#include <stdio.h>
#include <string.h>
int main()
{
	char tav,str[20];
	int i,max;
	puts("Enter a string: ");
	gets(str);
	printf("Enter a char: ");
	scanf("%c",&tav);
	max = strlen(str);
	for (i = 0; i <= max; i++)
	{
		if (str[i] != tav)
		{
			printf("%c",str[i]);
		}
	}

}
