#define N 2
#include <stdio.h>
int main()
{
	char st[N][20] = {"Try This","Check Up"};
	int i,j;
	for(i=0;i<N;i++)
	{
		printf("%c",st[i][0]);
		for(j=0;st[i][j] != '\0';j++)
		{
			if(st[i][j] == ' ')
			printf(".%c\n",st[i][j+1]);
		}
	}
	
}
