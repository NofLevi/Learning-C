#include <stdio.h>
#include <conio.h>
int main()
{
	int count,end = 20;
	LOOP:
	for (count = count;count <= end; count++)
	{
		printf("%c = %d\n",count,count);
	}
	if (end<=255)
	{
	getch();
	end = count + 19;
	goto LOOP;
	}
	else 
	{
		return 0;
	}
}
