#include <stdio.h>
#include <stdlib.h>

typedef struct node{
	int num;
	struct node *next;
}Node;

int func(Node *head)
{
	int count = 0
	node *pos=head;
	do{
	int n=pos->num;
	int i;
	for(i=0;i<n;i++)
	pos = pos-> next ;
	count++;
	}
	while(pos!=head);
}
