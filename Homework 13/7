#include <stdio.h>
#include <string.h>
int main()
{
	char str1[40],str2[40];
	int i,i2=1;
	puts("Enter a string");
	gets(str1);
	for (i=0;i<40;i++)
	{
		str2[0]= str1[0];
		if (str1[i]==' ')
		{
			str2[i2]= str1[i+1];
			str2[i2 = i2 + 1];
		}
	}	
	puts(str2);
}
