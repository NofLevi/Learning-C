#include <stdio.h>
#include <string.h>
int main()
{
	char str[20];
	int i;
	puts("Enter a string");
	gets(str);
	for (i=0;i<20;i++)
	{
		if (str[i]>=97 && str[i]<=122)
		{
		str[i]= str[i] - 32;
		}
	}
	puts(str);
}
