#include <stdio.h>
void main()
{
    int num,count; 
    printf("Enter 1 integer\n");
    for (count = 1;count<=8;count++)
    {
       
        scanf("%d",&num);
        if(num>100)
        {
        printf("%d ",count);
        }
    }
}
