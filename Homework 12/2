#include <stdio.h>
int main()
{
int count,count2,times;
	for(count = 1; count<=100; count++)
	{
		times = 0;
		for (count2 = 1;count2 <= count;count2++)
		{
			if (count%count2==0)
			{
					times = times + 1;
			}
		}
		if (times<=2)
		{
				printf("%d ",count);
		}
	}
}
