#include <stdio.h>
int main()
{
	int n,i,i2,sum=1;
	printf("Enter 1 number: ");
	scanf("%d",&n);
	for (i = 1; i <= n; i++)
	{
		sum = 1;
		for (i2 = 1;i2 <= i; i2++)
		{
		sum*= i;	
		}
		printf("%d ",sum);
	}
}
