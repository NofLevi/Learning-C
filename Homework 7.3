#include <stdio.h>
void main()
{
    int a,b,c;
    printf("Enter 2 numbers: ");
    scanf("%d%d",&a,&b);
    c = a + b;
    while ((c<=a)&&(c>))
    {
        c--;
        if (a<b)
        {
            
            printf("%d ",c);
            
        }
        else
        {
            printf("%d ",c);
        }
    }

}
