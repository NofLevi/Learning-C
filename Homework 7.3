#include <stdio.h>
void main()
{
  int a, b, c;
  printf ("Enter 2 numbers:\n");
  scanf ("%d",&a);
  scanf("%d",&b);
  c = a + b;
  if (a < b)
    {
      while ((c <= b)&&(c>=a))
	{
	  printf ("%d ", c);
	  c--;
	}

    }
  else if (b<=a)
    {
      while ((c <= a)&&(c>=b))
	{
	  printf ("%d ", c);
	  c--;
	}
    }
	else
	{
	    printf("the sum is:%d and the numbers between are",c,b);
	}
    }
    
