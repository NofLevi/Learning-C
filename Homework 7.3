#include <stdio.h>
void main()
{
  int a, b, c;
  printf ("Enter 2 numbers:\n ");
  scanf ("%d%d", &a, &b);
  c = a + b;
  if (a < b)
    {
      while (c <= b)
	{
	  printf ("%d ", c);
	  c--;
	}
	
    }
  else if (b<a)
    {
      while (c <= a)
	{
	  printf ("%d ", c);
	  c--;
	}
    }
}	
